from sqlalchemy import create_engine\nfrom sqlalchemy.ext.declarative import declarative_base\nfrom sqlalchemy.orm import sessionmaker\n\n# Database connection details\nDATABASE_URL = 'sqlite:///./test.db'  # You can change this to your actual database URL\n\n# Set up the database engine with connection pooling\nengine = create_engine(DATABASE_URL, connect_args={'check_same_thread': False}, pool_size=5, max_overflow=10)\n\nBase = declarative_base()\n\n# Create a configured "Session" class\nSessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)\n\n# Dependency to get session\ndef get_db():\n    db = SessionLocal()\n    try:\n        yield db\n    finally:\n        db.close()\n\n# Example of a model class\nclass ExampleModel(Base):\n    __tablename__ = 'example'  # Change the table name as needed\n\n    id = Column(Integer, primary_key=True, index=True)\n    name = Column(String, index=True)\n\n# Create the database tables\nBase.metadata.create_all(bind=engine)\n