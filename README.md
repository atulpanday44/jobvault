# Job Vault API

This is a FastAPI project for managing job listings.

## Requirements

- Python 3.8 or later

## Installation

```bash
pip install -r requirements.txt
```

## Run the API

```bash
uvicorn main:app --reload
```

## Docker

To run the application in a Docker container, use:

```bash
docker-compose up --build
```
